# 아이엠영어수학학원 홈페이지 — 작업 문서

> 이 문서 하나로 다른 도구(Claude Code, Cursor 등)에서 작업을 이어갈 수 있습니다.
> Claude Code를 쓰신다면 파일명을 `CLAUDE.md`로 바꾸면 자동으로 컨텍스트로 읽힙니다.

**버전** v8 · **최종 갱신** 2026-08-12 · **담당** 박요엘

---

## 1. 프로젝트 개요

| 항목 | 내용 |
|---|---|
| 단체명 | 아이엠영어수학학원 |
| 대표자 | 김미경 |
| 주소 | 경북 구미시 산호대로23길 36 옥계 다운타운 4층 |
| 운영시간 | 오후 1:00 ~ 오후 10:30 |
| 대표 전화 | **010-5547-5380** (사이트 전체 노출) |
| 의뢰 접수 연락처 | 010-7334-9766 (대표자 김미경) — 미사용 |
| 이메일 | iamhagwon@naver.com |
| 카카오톡 채널 | http://pf.kakao.com/_xlsqPb/chat |
| 링크 모음 | https://litt.ly/iamhagwon |
| 등록번호 | 구미교육지원청 등록번호 제842호 |
| 희망 도메인 | www.iamhagwon.com (**미확정 · 선점 여부 미조회**) |

---

## 2. 확정된 결정사항 (변경 시 상의 필요)

1. **문의·상담 신청 폼 없음.** 모든 CTA는 `tel:01055475380` 전화 또는 카카오톡 채널.
2. **원페이지 구성.** 몰입캠프만 별도 페이지(`camp.html`).
3. 색상은 **로고에서 추출한 2색만** 사용. 파란색 금지.
4. 히어로 카피는 원고의 HOME 문구 그대로. 임의 수정 금지.
5. **사이트 문구는 전부 원장님 제공 원고(`홈페이지.txt`)에서 그대로 가져온 것.** 제작자가 지어낸 문장은 없음. 새 문구가 필요하면 반드시 원장님 확인.
6. 학생 얼굴이 식별되는 사진은 사용하지 않음 (모자이크·이모지 처리분만 사용).

---

## 3. 파일 구조

```
/
├── index.html      메인 원페이지 (1,182줄 / 약 72KB)
├── camp.html       몰입캠프 페이지 (502줄 / 약 28KB)
├── CLAUDE.md       이 문서
├── README.md
└── images/         사진·로고·파비콘 25개 (약 1.0MB)
    ├── logo.png            208×240   헤더 로고 (배경 투명)
    ├── favicon.png         64×64
    ├── declaration-1~4     학생 선언문 (#declaration)
    ├── planner-1~2         학습 플래너 (#system)
    ├── doodimbook-1~2      두딤북 (#system)
    ├── award-1~3           성장 시상식 (#evaluation)
    ├── class-1~6           수업 현장 (#gallery)
    ├── camp-scene-1~2      캠프 현장 (camp.html)
    └── camp-review-1~4     캠프 후기 손글씨 (camp.html)
```

**구성**

- CSS·JS는 전부 인라인. 사진은 v8에서 `images/` 폴더로 분리함 (아래 참고)
- 외부 의존: Pretendard 폰트 CDN 1개
  `https://cdn.jsdelivr.net/gh/orioncactus/pretendard@v1.3.9/dist/web/static/pretendard.min.css`
- 빌드 도구 없음. 파일을 브라우저로 열면 그대로 동작

**v8에서 바뀐 점 — 사진 외부 분리 완료**

v7까지는 사진·로고·파비콘이 base64 data URI로 HTML 안에 박혀 있어 index.html이 1.17MB였습니다. v8에서 `images/` 폴더로 빼고 상대경로 참조로 바꿨습니다.

| | v7 | v8 |
|---|---|---|
| index.html | 1,208,956B (1.15MB) | 73,453B (**94% 감소**) |
| camp.html | 279,467B | 28,381B (**90% 감소**) |

사진은 `images/`에 총 약 1.0MB로 남지만, 전부 `loading="lazy"`라 첫 화면에서는 내려받지 않습니다. 로고·파비콘(46KB)만 즉시 로딩됩니다.

- 로고·파비콘은 두 파일에 각각 내장돼 있었으나 동일 이미지라 하나로 합침. 두 페이지가 캐시를 공유합니다
- 각 `<img>`에 원본 크기 `width`·`height`가 있습니다. **지연 로딩 시 레이아웃 밀림(CLS) 방지용이니 지우지 마세요**
- 사진을 교체할 때는 `images/`의 파일만 바꾸면 됩니다. HTML은 건드릴 필요 없습니다. 단 **가로·세로 비율이 달라지면 `width`·`height` 속성도 같이 고쳐야 합니다**

---

## 4. 디자인 시스템

### 4-1. 컬러 (로고에서 직접 추출)

| 토큰 | 값 | 용도 |
|---|---|---|
| `--brand` | `#263238` | 로고 차콜. 상단 전화버튼, 캠프 배너, 하단 CTA 배경 |
| `--brand-soft` | `#eef1f3` | 차콜 파스텔 |
| `--accent` | `#db7723` | 로고 오렌지. 메인 버튼, 형광펜, 강조 |
| `--accent-deep` | `#b25c11` | **밝은 배경 위 오렌지 텍스트용** (명도대비 확보) |
| `--accent-soft` | `#fdf1e7` | 오렌지 파스텔 |
| `--cream` | `#f7f1e5` / `--sage` `#ecf1ee` | 보조 파스텔 |
| `--ink` `#1c252a` / `--ink2` `#4d545d` / `--ink3` `#8b929b` | | 텍스트 3단계 |
| `--bg` `#fffdf8` / `--white` `#ffffff` / `--line` `#ece9e2` | | 배경·구분선 |

**규칙**

- 오렌지는 넓은 면적에 쓰지 않는다. 포인트로만.
- 밝은 배경 위 오렌지 글자는 `--accent` 아닌 **`--accent-deep`**.
- 카카오 버튼만 예외적으로 `#FEE500` (카카오 공식색).

### 4-2. 타이포 (v7에서 통일)

| 대상 | 크기 | 줄간격 | 자간 |
|---|---|---|---|
| 본문 (`.card p` `.box p` `.level p` `.gitem p` `.q-body p`) | **15.5px** | **1.78** | -0.015em |
| `.sec-desc` | 16px | 1.8 | — |
| `.philo p` | clamp(15.5, 2vw, 17) | 1.9 | — |
| `h2.sec-title` | clamp(26, 4.2vw, 40) | 1.32 | -0.038em |
| 카드 제목 | 18~20px | 1.42 | -0.035em |
| `.hero h1` | clamp(27, 5.4vw, 54) | 1.3 | -0.045em |

- 폰트: **Pretendard**
- `word-break: keep-all` 전역 — 한글 단어 중간 줄바꿈 방지. **지우지 말 것**
- `text-wrap: balance` (h1~h4) — 제목 마지막 줄이 짧게 남는 현상 방지
- 숫자는 `font-variant-numeric: tabular-nums` (교습비 표·전화번호)

### 4-3. 간격 체계

| 대상 | 값 |
|---|---|
| 큰 카드 그리드 (`.cards3 .levels .two .evals .news .ngrid`) | `gap:18px` |
| 중간 그리드 (`.grid8 .gal .n4`) | `gap:16px` |
| 썸네일 (`.mini`) | `gap:12px` |
| 섹션 상하 패딩 | `clamp(54px, 7vw, 88px)` |
| `.sec-head` 하단 여백 | `clamp(32px, 4.5vw, 48px)` |
| 모서리 | `--r: 18px` / `--r-lg: 24px` |
| 콘텐츠 최대폭 | `--max: 1120px` |

### 4-4. 브레이크포인트

| 폭 | 변화 |
|---|---|
| `1160px` 이하 | 상단 메뉴 → **햄버거 메뉴**, 전화버튼 우측 정렬 |
| `1000px` 이하 | 성장시스템 8칸 → 2열 |
| `900px` 이하 | 2단 → 1단 |
| `880px` 이하 | 3열 카드 → 1열, 갤러리 4열 → 2열 |
| `820px` 이하 | 교습비 2열 → 1열 |
| `768px` 이하 | **하단 고정바 노출** (전화 / 카카오톡) |
| `560px` 이하 | 로고·전화버튼 축소 |
| `360px` 이하 | 헤더 학원명 숨김 |

---

## 5. 페이지 구조

### index.html

| # | 앵커 ID | 섹션 | 내용 출처 |
|---|---|---|---|
| — | `#hd` | 고정 헤더 | **하단 라인 상시 표시**, 스크롤 시 배경 진해짐 |
| 1 | `#hero` | 공부의 판을 뒤집다 | 원고 1. HOME |
| 2 | `#philosophy` | 교육철학 | 원고 2 |
| 3 | `#declaration` | 아이들의 선언 | 학생 손글씨 4장 |
| 4 | `#why` | 왜 아이엠인가 (3단계) | 원고 3 |
| 5 | `#system` | 성장시스템 8단계 + 플래너·두딤북 4장 | 원고 4 |
| 6 | `#curriculum` | 영어 / 수학 탭 + 수학 평가시스템(A~D) | 원고 5·6·7 |
| 7 | `#allcare` | 초·중등 올케어 | 원고 8 |
| 8 | `#evaluation` | NELT·TOSEL·단원/월말 + 더 큰 무대 + 성장 시상식(사진 3장) | 원고 11·12·13 |
| 9 | `#tuition` | 교습비 (정규 16 / 특강 7, 2열) | 교습비 표 이미지 |
| 10 | `#gallery` | 아이엠의 수업 현장 (사진 6장) | 제공 사진 |
| 11 | `#news` | 공지사항 | **빈 자리 3개** |
| 12 | `#faq` | 자주 묻는 질문 17개 | 원고 14 |
| 13 | `#visit` | 오시는 길 | — |
| 14 | `#camp` | 몰입캠프 배너 → camp.html | 원고 9 |
| — | — | 상담 CTA (전화·카카오·링크) | 원고 15 |
| — | — | 푸터 (등록번호 제842호) | — |
| — | — | 모바일 하단 고정바 / ↑맨 위로 버튼 | — |

### camp.html

히어로 → 몰입캠프가 남기는 네 가지 → 세 가지 캠프 → 캠프 현장(사진 2장) → 학생 손글씨 후기 4장 → CTA → 푸터
※ 원고 9·10에서 전부 가져옴.

---

## 6. 주요 컴포넌트 클래스

| 클래스 | 용도 |
|---|---|
| `.wrap` / `.narrow` | 1120px / 820px 컨테이너 |
| `.eyebrow` `.eyebrow.o` | 섹션 라벨 (오렌지 / 차콜) |
| `.sec-head` `.sec-title` `.sec-desc` | 섹션 헤더 세트 |
| `.band-white` `.band-warm` | 섹션 배경 변주 |
| `.btn` + `.btn-primary` `.btn-kakao` `.btn-ghost` `.btn-white` | 버튼 |
| `.card` `.gitem` `.level` `.box` `.news-item` `.prog` | 카드류 (전부 flex column, 높이 균등) |
| `.grid8` | 성장시스템 8칸 |
| `.flow` | 흐름 칩 (동기 → 계획 → …) |
| `.tabs` `.tab` `.panel` | 영어/수학 탭 |
| `.tiers` `.tier` | 월말평가 A/B/C/D |
| `.price-two` `.price-wrap` `table.price` | 교습비 |
| `.gimg` | 사진 (4:3 크롭, 클릭 확대) |
| `.ncard` `.ngrid` | 손글씨 카드 (3:2 박스, 잘림 없음) |
| `.n4` `.mini` | 4열 / 3열 썸네일 |
| `.pmodal` | 사진 확대 라이트박스 |
| `.q` `.q-btn` `.q-body` | FAQ 아코디언 |
| `.callbar` `.totop` | 모바일 하단바 / 맨 위로 |
| `.rv` | 스크롤 진입 페이드업. **새 요소엔 이 클래스만 붙이면 됨** |

---

## 7. JavaScript (index.html 하단 `<script>` 하나)

1. 헤더 스크롤 상태 토글 + ↑맨 위로 버튼 노출(900px 이상)
2. 히어로 형광펜 애니메이션 트리거
3. `IntersectionObserver` 스크롤 리빌 (`.rv`)
4. 햄버거 모바일 메뉴 토글
5. 커리큘럼 탭
6. FAQ 아코디언
7. 사진 라이트박스 (`.gimg`, `.ncard` 클릭 / ESC 닫기)
8. **스크롤 스파이** — 현재 섹션 메뉴에 오렌지 밑줄. 화면 중앙 띠(`rootMargin -45%/-50%`)에 걸친 섹션 중 가장 위의 것 하나만 활성화하고, 없으면(맨 위·푸터) 전부 해제. 홈 메뉴는 빈 앵커 `#top`을 가리키므로 `#hero`에 따로 연결해 둠

---

## 8. CSS 구조 주의사항 ⚠️

`<style>` 맨 끝에 **`/* ===== v7 · 디테일 정리 ===== */` 블록**이 있습니다.
여기서 앞쪽 규칙들을 덮어쓰고 있습니다 (타이포·간격·카드 정렬·헤더 라인 등).

**스타일을 수정할 때는 이 블록을 먼저 확인하세요.** 위쪽 원본 규칙만 고치면 반영되지 않습니다.
정리하려면 v7 블록의 값을 위쪽 원본 규칙에 병합하고 블록을 지우면 됩니다.

---

## 9. 남은 작업

### A. 원장님 확인 필요
- [ ] **교습비 표 중복** — `중등올케어1/2`가 두 번, 금액 다름 (400,000·500,000 / 450,000·600,000). 앞의 것이 초등올케어인지 확인. **원본 그대로 넣어둔 상태**
- [ ] **운영 요일** — 원고에 시간(오후 1:00~10:30)만 있고 요일이 없음. 검색 결과에 영업시간을 띄우려면 필요 (`index.html` JSON-LD의 `openingHoursSpecification`)
- [ ] 도메인 `iamhagwon.com` 선점 여부 · 최종 확정
- [ ] 사업자등록번호 (현재 푸터에 학원등록번호만 표기)
- [ ] 로고 원본 파일 (ai/svg) — 현재 PNG 투명 처리본 사용
- [ ] **가로형 로고** 유무 (현재 세로 비율 0.87:1이라 헤더에서 답답함)
- [ ] SNS 계정 주소 (블로그·인스타·유튜브) — 현재 카카오채널·링크모음만 연결
- [ ] 학생 사진 초상권 동의 여부 (동의 시 미사용 사진 4장 추가 가능)

### B. 자료 받으면 바로 가능
- [ ] 공지사항 실제 3건 (`#news` 빈 자리)
- [ ] 학원 공간 사진 (입구·강의실·자습실) — 현재 갤러리는 수업 장면만
- [ ] 유학캠프 자료 (별도 프로그램이면 페이지 추가)

### C. 기능 추가
- [ ] **지도 임베드** — 카카오맵 JS SDK (앱키 발급 필요). 현재 외부 링크 버튼만
- [ ] **게시판** — 공지 CRUD. 현재 하드코딩. 관리자 직접 작성이 필요하면 백엔드(Supabase 등) 필요
- [ ] **방문자 통계** — GA4 스니펫 `</head>` 직전 삽입 (측정 ID 필요)
- [ ] **OG 이미지** (카톡 공유 썸네일 1200×630) — 메타 태그 자리는 TODO 주석으로 잡아둠. 이미지 파일만 만들면 됨
- [x] ~~기타 공유·검색 메타~~ — v8에서 `og:site_name`·`og:locale`·`twitter:card`·`theme-color`·JSON-LD 완료

### D. 배포 전 필수
- [x] ~~**사진 외부 파일 분리**~~ — v8 완료 (1.17MB → 61KB)
- [ ] 도메인 구입 · 호스팅 (정적이라 Netlify / Vercel / Cloudflare Pages 무료 티어로 충분)
- [ ] **도메인 확정 후 처리** — 두 HTML의 `TODO 도메인 확정 후` 주석 자리에 `og:url`·`canonical`·`og:image` 절대경로 채우고, JSON-LD의 `logo`도 절대경로로. `sitemap.xml`·`robots.txt`도 이때
- [ ] **`images/` 캐시 헤더** — 호스팅 정하면 장기 캐시(`max-age=31536000`) 설정. 사진이 1MB라 효과 큼
- [ ] 개인정보처리방침 (GA4 넣으면 필요)
- [ ] 실기기 테스트 — iOS Safari / Android Chrome에서 `tel:` · 카카오 링크 동작 확인
- [ ] 로고 PNG → SVG

---

## 10. 코딩 컨벤션

- 바닐라 HTML/CSS/JS. 프레임워크·빌드 없음
- 색은 반드시 CSS 변수 사용. 하드코딩 금지
- 새 섹션 추가 패턴:
  ```html
  <section id="새이름" class="band-white">
    <div class="wrap">
      <div class="sec-head rv">
        <span class="eyebrow">라벨</span>
        <h2 class="sec-title">제목</h2>
        <p class="sec-desc">설명</p>
      </div>
      <!-- 애니메이션 붙일 요소마다 class="rv" -->
    </div>
  </section>
  ```
  섹션에 `id`를 주면 **스크롤 스파이가 자동으로 잡습니다.** 메뉴 링크만 추가하면 됨
- 사진 추가: 파일을 `images/`에 넣고
  `<button class="gimg"><img src="images/이름.jpg" alt="설명" loading="lazy" width="원본가로" height="원본세로" decoding="async"></button>`
  → 라이트박스 자동 연결. `width`·`height`는 원본 크기를 그대로 (레이아웃 밀림 방지)
- `prefers-reduced-motion` 대응 완료. 새 애니메이션도 동일 처리
- localStorage / sessionStorage 사용 금지

---

## 11. 원본 자료

| 자료 | 내용 |
|---|---|
| `홈페이지.txt` | 원장님 제공 원고 15개 섹션 (사이트 문구 전부의 출처) — **저장소에 없음.** 문구를 고칠 일이 있으면 먼저 이 파일부터 받아야 함 |
| 교습비 표 이미지 | 정규 16 / 특강 7 = 23행 |
| 로고 PNG | 차콜+오렌지 |
| 사진 | 시상식 16장 · 수업현장 11장 · 학생 선언문 15장 · 캠프 후기 13장 · 플래너 3장 · 두딤북 7장 |

---

## 12. 작업 이력

| 버전 | 내용 |
|---|---|
| v1 | 원페이지 초안. 문의폼 제외, 전화 CTA 중심. 임시 블루 팔레트 |
| v2 | 로고 색(차콜/오렌지) 전면 적용, 헤더 로고 삽입, CSS 변수명 정리 |
| v3 | 홈 메뉴 추가, 캠프 맨 뒤 배치, 영상 삭제, **제작자가 지어낸 문구 전부 제거** |
| v4 | 원장님 원고 전체 반영 (교육철학·성장시스템·커리큘럼·올케어·평가·교습비·FAQ 17) |
| v5 | 사진 전면 반영 (수업현장·선언문·플래너·두딤북·시상식·캠프후기) + 라이트박스 |
| v6 | 길이 18,300 → 15,000px, 파일 1.7MB → 1.2MB로 압축. 교습비 2열. 맨 위로 버튼 |
| v7 | **헤더 하단 라인 상시 표시**, 타이포·간격 체계 통일, 카드 높이 균등, 스크롤 스파이, 모바일 헤더 정렬 |
| v8 | **저장소에 v7 반영** (그전까지 저장소엔 자리표시자만 있는 645줄 골격뿐이었고 camp.html이 없어 링크가 404였음). **사진 25장 `images/` 분리 → index.html 1.17MB→61KB**. 스크롤 스파이·라이트박스 버그 수정, 공유·검색 메타 보강 |
